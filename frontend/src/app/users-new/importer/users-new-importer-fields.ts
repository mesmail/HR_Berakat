import { UsersNewModel } from 'src/app/users-new/users-new-model';

const { fields } = UsersNewModel;

export default [
  fields.email,
  fields.firsrtName,
  fields.secondName,
  fields.phoneNumber,
  fields.roles,
];
