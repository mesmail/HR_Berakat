import { TrainingCertificatesModel } from 'src/app/training-certificates/training-certificates-model';

const { fields } = TrainingCertificatesModel;

export default [
  fields.id,
  fields.trainingCertificates,
  fields.createdAt,
  fields.updatedAt,
];
