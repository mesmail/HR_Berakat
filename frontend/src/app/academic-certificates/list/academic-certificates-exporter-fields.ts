import { AcademicCertificatesModel } from 'src/app/academic-certificates/academic-certificates-model';

const { fields } = AcademicCertificatesModel;

export default [
  fields.id,
  fields.academicCertificates,
  fields.createdAt,
  fields.updatedAt,
];
