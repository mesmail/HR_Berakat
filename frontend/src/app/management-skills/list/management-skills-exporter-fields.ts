import { ManagementSkillsModel } from 'src/app/management-skills/management-skills-model';

const { fields } = ManagementSkillsModel;

export default [
  fields.id,
  fields.managementSkills,
  fields.createdAt,
  fields.updatedAt,
];
