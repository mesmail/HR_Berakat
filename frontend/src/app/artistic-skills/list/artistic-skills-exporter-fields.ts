import { ArtisticSkillsModel } from 'src/app/artistic-skills/artistic-skills-model';

const { fields } = ArtisticSkillsModel;

export default [
  fields.id,
  fields.artisticSkills,
  fields.createdAt,
  fields.updatedAt,
];
